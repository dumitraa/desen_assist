[backend]               ; ------------------------------------
; FastAPI base
url     = http://localhost:8000      
; seconds before the POST gives up
timeout = 1                         

[layers]                ; ------------------------------------
watch   = FB pe C LES, TRONSON_JT, BRANS_FIRI_GRPM_JT, STALP_JT, LINIA_JT

[auth]                  
token   = demo-secret-123

[validation]            ; (optional quick rules - plugin enforces them client-side)
; any field listed here must never be NULL
not_null = COD, DENUMIRE
; numeric limits   syntax:  fieldname.min   /  fieldname.max
LUNGIME_JT.min = 0

[presence]
idle_seconds = 300

[metrics]
monthly_km_target = 0    ; TODO set real target
